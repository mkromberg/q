 old_idiv←{    ⍝ ⌊⍺÷⍵
     ⎕CT←0
     (≢⍺)<≢⍵:,0
     ((≢⍺)=≢⍵)∧⊃(⍺≠(≢⍺)↑⍵)/⍺<(≢⍺)↑⍵:,0
     c←(≢⍺)-m←0⌈(≢⍺)-16
     d←(≢⍵)-n←0⌈(≢⍵)-16
     t←¯16⍕(10⊥c↑⍺)÷10⊥d↑⍵
     q←((m-n)+1+⍎(1+t⍳'E')↓t)↑⎕D⍳t[1],5↑3↓t
     a←(-≢⍺)↑⍵ itimes q
     assert 0≤s←⍺ icmp a:
     0=s:q
     r←⍺ iminus a
     0<⍵ icmp r:q
     q iplus r ∇ ⍵
 }
